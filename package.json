{
  "name": "dsa-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix client",
    "dev": "npm run dev --prefix client",
    "server": "npm run dev --prefix server"
  }
}
